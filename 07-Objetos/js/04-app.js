const producto = {
    nombre: 'monitor de 20 pulgadas',
    precio: 300,
    disponible: true,
}

console.log(producto.nombre);

// destructuring
const {nombre, disponible} = producto;
const {precio} = producto;
console.log(nombre, precio);
console.log(precio);

// no entendi mucho esto la verdad !!!!!!!!!!!!!!!!!!!!


