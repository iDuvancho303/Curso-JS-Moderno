// crear objectos en Javascript
const nombre = "monitor de 20 pulgadas";
const precio = 300;
const disponible = true;

// un objeto agrupa todo en una sola variable...
// de este modo no tienes que crear muchas variables

// object literal
const producto = {
    nombre: 'monitor de 20 pulgadas',
    precio: 300,
    disponible: true,
}

console.log(producto);
// asegurate de usar ":" y las "," para anadir mas valores